name: 🧪 Test Suite

on:
  push:
    branches: [ main]
  pull_request:
    branches: [ main]
  workflow_dispatch:

jobs:
  test:
    name: "🧪 Test (${{ matrix.python-version }})"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
      fail-fast: false

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐍 Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: 📦 Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: 🔧 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[dev]
        cd auth_server && pip install -e .[dev]

    - name: 🔍 Check dependencies
      run: |
        pip check

    - name: Get latest schema version
      id: get-schema
      uses: ./.github/actions/get-schema-version
      with:
        github-token: ${{ secrets.JARVIS_RELEASE_ACCESS_TOKEN }}

    - name: 📦 Install uv
      run: |
        curl -LsSf https://astral.sh/uv/install.sh | sh
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Import schema
      id: import-schema
      run: |
        echo "Using SCHEMA_VERSION: ${{ steps.get-schema.outputs.schema-version }}"
        cd packages/models
        uv run import-schemas --tag ${{ steps.get-schema.outputs.schema-version }} --output-dir . --token ${{ secrets.JARVIS_RELEASE_ACCESS_TOKEN }}

    - name: 🧪 Run unit tests
      run: |
        pytest tests/unit -v

    - name: 🔗 Run integration tests  
      run: |
        pytest tests/integration -v

    - name: � Run auth_server tests
      run: |
        cd auth_server
        pytest tests/integration -v

    - name: 📊 Generate coverage report
      run: |
        pytest --cov=registry --cov-report=xml --cov-report=html
        cd auth_server && pytest --cov=auth_server --cov-report=xml --cov-report=html --cov-append

    - name: 📤 Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

    - name: 📄 Upload coverage reports
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report-${{ matrix.python-version }}
        path: htmlcov/

    - name: 📋 Upload test reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports-${{ matrix.python-version }}
        path: tests/reports/

  lint:
    name: "🔍 Code Quality"
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install bandit black isort flake8

    - name: 🔒 Security check with bandit
      run: |
        bandit -r auth_server/ -f json -o bandit-report-auth.json || true
        bandit -r registry/ -f json -o bandit-report.json || true

    - name: 📋 Upload security report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: |
          bandit-report.json
          bandit-report-authrt
        path: bandit-report.json

  
  summary:
    name: "📋 Test Summary"
    runs-on: ubuntu-latest
    needs: [test, lint]
    if: always()

    steps:
    - name: 📋 Test Results Summary
      run: |
        echo "## 🧪 Test Results Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Registry Tests | ${{ needs.test.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "| Auth Server Tests | Included in Registry Tests |" >> $GITHUB_STEP_SUMMARY
        echo "| Code Quality | ${{ needs.lint.result }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [[ "${{ needs.test.result }}" == "success" && "${{ needs.lint
        
        if [[ "${{ needs.test.result }}" == "success" && "${{ needs.lint.result }}" == "success" && "${{ needs.domain-tests.result }}" == "success" ]]; then
          echo "✅ All tests passed! 🎉" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ Some tests failed. Please check the logs." >> $GITHUB_STEP_SUMMARY
        fi 