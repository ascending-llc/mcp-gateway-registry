# Unified Vector Database Configuration
# ==========================================

# Core Configuration
# ------------------
# Supported values: "weaviate"
VECTOR_STORE_TYPE=weaviate

# Supported values: "openai", "aws_bedrock"
EMBEDDING_PROVIDER=aws_bedrock


# Weaviate Configuration
# ----------------------
# Required when VECTOR_STORE_TYPE=weaviate
WEAVIATE_HOST=127.0.0.1
WEAVIATE_PORT=8099
WEAVIATE_API_KEY=test-secret-key
WEAVIATE_COLLECTION_PREFIX=  # Optional: prefix for collection names

# OpenAI Embedding Configuration
# ------------------------------
# Required when EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=text-embedding-ada-002


# AWS Bedrock Embedding Configuration
# -----------------------------------
# Required when EMBEDDING_PROVIDER=aws_bedrock
AWS_REGION=us-east-1
EMBEDDING_MODEL=amazon.titan-embed-text-v2:0 # Default Bedrock embedding model

# Optional: If not provided, AWS SDK will use default credentials chain
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key


# Performance Tuning (Optional)
# -----------------------------
WEAVIATE_SESSION_POOL_CONNECTIONS=10
WEAVIATE_SESSION_POOL_MAXSIZE=10
WEAVIATE_INIT_TIME=5
WEAVIATE_QUERY_TIME=10
WEAVIATE_INSERT_TIME=10


# Configuration Examples
# ======================

# Example 1: Weaviate + AWS Bedrock
# ---------------------------------
# VECTOR_STORE_TYPE=weaviate
# EMBEDDING_PROVIDER=aws_bedrock
# WEAVIATE_HOST=127.0.0.1
# WEAVIATE_PORT=8099
# AWS_REGION=us-east-1
